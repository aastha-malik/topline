# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Settings / validation ---
pydantic>=2.12.5,<3.0.0
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database (Supabase Postgres over the session pooler) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# --- Auth / Google OAuth / Gmail ---
authlib==1.4.0
httpx==0.28.1
google-api-python-client==2.157.0
google-auth>=2.56.0,<3.0.0
google-auth-oauthlib==1.2.1

# --- Backend-only agent structured outputs ---
google-genai==2.20.0

# --- Secrets at rest ---
cryptography==44.0.0

# --- Invoice PDF extraction (text first; OCR is an optional fallback) ---
pypdf==5.1.0

# --- Payments (optional confirmation source) ---
razorpay==2.0.1

# --- Scheduling (registration surface only in this milestone) ---
apscheduler==3.11.0
